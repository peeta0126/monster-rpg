# Handoff — Monster RPG

이 문서는 다음 작업자(사람 또는 에이전트)가 이 저장소를 이어받을 때 필요한 최소한의 맥락을 정리한 인수인계 문서입니다.
설치/실행 절차, 화면 경로, 파일별 역할 등 "정적인 사실"은 [README.md](README.md)에 이미 정리되어 있으므로 여기서는 반복하지 않고,
**지금 코드가 어떤 상태인지 / 어디를 만지면 무엇이 깨지는지 / 다음에 무엇을 해야 하는지**에 집중합니다.

- 기준 커밋: `b690234` (feat: 렌더링/Phaser 씬 예외에 대한 에러 바운더리 추가)
- 기준 브랜치: `main`
- 작성일: 2026-08-07

---

## 1. 한 줄 요약

React 19 + TypeScript + Phaser 3 기반 브라우저 몬스터 RPG. **베이스캠프 → 무한의 탑(50층) → 엔딩**까지 이어지는
메인 진행선이 코드상 끝까지 연결되어 있고, 여기에 숲 탐험 / 몬스터 육성 / 공방 제작이 곁가지로 붙어 있습니다.
프론트엔드 단독으로도 게스트 모드로 완주 가능하며, `server/`(Express + Prisma + SQLite)는 로그인과 세이브 동기화를 담당하는 선택 요소입니다.

---

## 2. 지금 동작하는 것 (검증된 범위)

메인 진행선은 Playwright 자동 플레이로 **1층부터 50층 보스(오름) 격파 → `/ending` 화면 도달**까지 실제 브라우저에서 완주를 확인했습니다.
개발자 프리셋 구성(레벨 50 + 만렙 엘리트 아티팩트 + 물약)으로 50층을 이길 수 있는지도 별도 테스트로 확인합니다.
(스크립트: `e2e/`, 아래 5장 참고)

| 영역 | 상태 | 비고 |
|---|---|---|
| 로그인 게이트 | 동작 | 게스트 / 로그인 / 개발자 코드 3경로. `/admin`만 게이트 예외 |
| 베이스캠프 (Phaser) | 동작 | WASD·방향키 이동, `E`로 탑/숲/공방/NPC 상호작용, `Tab` 메뉴 |
| 무한의 탑 전투 | 동작 | 1~50층. 10·20·30·40층 보스, 50층 최종보스 오름 |
| 엔딩 | 동작 | 50층 승리 오버레이의 "정수를 들고 마을로" → `/ending`. 도달 시 `tower_cleared` 플래그 저장 |
| 숲 탐험 | 동작 | 노드 탐색, 몬스터 조우, 재료 획득 |
| 공방 제작 | 동작 | 물약/아티팩트 제작 + 미니게임 2종(가위바위보, 방향키) |
| 아티팩트 장착/강화 | 동작 | 공/방/속/치명/속성/HP/경험치 보너스가 실제 전투 계산에 반영됨 |
| 세이브 | 동작 | zustand persist(`monster-rpg-player`), version=1 + migrate + normalizeState 이중 보정 |
| 서버 동기화 | 동작 | 로그인 시 서버 세이브 로드/업로드(4초 디바운스). 오프라인이어도 로컬 저장은 유지 |
| 에러 처리 | 동작 | `ErrorBoundary` + `AppErrorBridge`로 렌더/Phaser 씬 예외를 폴백 화면으로 |

---

## 3. 아키텍처에서 반드시 알아야 할 3가지

README의 파일 목록만 봐서는 안 보이는, 실제로 코드를 고칠 때 걸리는 지점들입니다.

### 3.1 React ↔ Phaser는 전역 이벤트 버스로만 통신한다

`src/shared/phaser/events.ts`의 `gameEvents`(Phaser EventEmitter)가 유일한 통로입니다. props도 ref도 아닙니다.

전투의 핵심 흐름은 **"로그 한 줄 → Q 입력 대기 → 다음 줄"** 의 왕복입니다.

```
BattlePage.sendLogAndWait(text)
  └─ emit BATTLE_LOG ─────────────▶ BattleScene.onBattleLog  (로그 박스 표시, logState="showing")
                                          │  사용자가 Q / Space / 하단 클릭
  ◀──────── emit BATTLE_LOG_ACK ──── BattleScene.onAdvance
  └─ Promise resolve → 다음 로그
```

**주의점**

- `sendLogAndWait`에는 5초 타임아웃 방어책이 있습니다(`BattlePage.tsx`). ACK가 안 와도 전투가 영구히 멈추지는 않지만,
  ACK 경로를 깨뜨리면 전투가 매 줄 5초씩 느려지는 형태로 조용히 망가집니다. 증상이 "전투가 갑자기 느려짐"이면 여기를 의심하세요.
- `BATTLE_READY` 이벤트를 받기 전까지 `isProcessing = true`로 조작을 막습니다. 씬 `create()`가 리스너를 등록하기 전에
  스킬을 쓰면 ACK를 영원히 못 받기 때문입니다. 이 가드를 지우지 마세요.
- `BattlePageWrapper`(`App.tsx`)가 `location.key`를 `key`로 넘겨 **매 전투마다 BattlePage를 완전히 재마운트**합니다.
  "다음 층"·"재도전"이 새 전투로 시작되는 건 이 덕분입니다.

### 3.2 층 번호는 URL이 아니라 라우터 state로만 전달된다

```ts
navigate("/battle", { state: { floor: 12, isCatchZone: false } })
```

`BattlePage`는 `location.state.floor`를 읽고, 없으면 **1층으로 폴백**합니다.
따라서 `/battle`을 주소창에 직접 치면 언제나 1층입니다. (자동화 스크립트를 짤 때 여기에 걸리기 쉽습니다.)

중간 층으로 진입하는 정식 경로는 베이스캠프에서 탑 앞(`TOWER_X=278, TOWER_Y=1010` 부근)에서 `E` →
`ENTER_BATTLE` 이벤트 → `TowerModal`에서 층 선택입니다. 선택 가능한 층은 `1`, 그리고 5의 배수 체크포인트와
`min(bestFloor + 1, 50)`입니다.

### 3.3 세이브는 "저장된 능력치를 믿지 않는다"

`src/shared/playerStore.ts`의 `normalizeOwnedMonster`는 로드할 때마다 **`id` + `level`만 신뢰**하고
`maxHp/attack/defense/speed`를 `recomputeStatsForLevel`로 다시 계산합니다(레벨당 HP+10/공격+3/방어+2/속도+2).

- 장점: `monsters.ts` 밸런스를 바꾸면 기존 세이브에도 즉시 반영됩니다. 마이그레이션이 대부분 공짜입니다.
- 함의: 세이브를 직접 만들 때 `{ id, level }`만 넣어도 나머지는 알아서 채워집니다(테스트용 세이브 주입이 쉬움).
- 함의: 능력치만 따로 저장해 두는 방식의 기능(예: 영구 버프)을 추가하려면 이 재계산을 우회할 별도 필드가 필요합니다.

세이브 구조를 바꿀 때는 `PERSIST_VERSION`을 올리고 `migrate`에 분기를 추가하세요. 기존 분기는 지우면 안 됩니다.

---

## 4. 전투 밸런스 (숫자를 만질 때 반드시 읽을 것)

데미지 공식은 단순합니다 (`src/battle/battleUtils.ts`).

```
damage = floor( (공격력 × 기술위력 / 방어력) × 속성배율 )    // 최소 1
```

곱연산이 아니라 **나눗셈 기반**이라, 방어력을 조금만 올려도 체감이 큽니다.
일반 층의 적 레벨은 `floor`와 같고, 보스층은 `floor + 3`입니다.

### 4.1 성장축이 세 개라는 전제

이 게임의 전투력은 **레벨 · 기술 · 장비** 세 축으로 올라갑니다. 셋 중 하나라도 막히면 나머지로
메꿀 수 없어 진행이 멈춥니다(실제로 예전에는 기술과 장비가 통째로 막혀 있었습니다 — 8장 참고).

| 축 | 오르는 방법 | 관련 코드 |
|---|---|---|
| 레벨 | 탑 전투 경험치. 출전 100% + 대기 파티원 50% | `battleUtils.gainExp`, `BattlePage`의 `BENCH_EXP_SHARE` |
| 기술 | 레벨업 시 학습표대로 습득, 4칸이 차면 가장 약한 기술과 교체. 조건 레벨에서 진화 | `monster/growth.ts`, `monster/learnset.ts` |
| 장비 | 숲·전투 드랍 재료로 제작 → 모루에서 레벨업/강화/합성 | `workshop/`, `shared/craftingUtils.ts` |

### 4.2 재료 순환 (막히면 게임이 멈추는 지점)

| 재료 | 나오는 곳 |
|---|---|
| 약초·열매·나무뿌리·나무판자·가죽 | 얕은 숲, 1~10층 전투 |
| 슬라임 추출물 | 얕은 숲, 깊은 숲 |
| 빛의 수정·철 조각·마법 가루 | 깊은 숲(11층~), 11층~ 전투 |
| 몬스터 정수·강화석 | 고대 숲(21층~), 21층~ 전투 |

**새 재료를 요구하는 레시피를 추가할 때는 반드시 이 표에 획득처를 먼저 만들어 두세요.**
`slime_extract`처럼 "퀘스트가 요구하는데 어디서도 안 나오는" 재료가 생기면 그 퀘스트와
그 뒤의 모든 제작이 조용히 사망합니다.

### 4.3 보스 실효 능력치 (현재 값)

| 층 | 보스 | 레벨 | HP | 공격 | 방어 | 배수(HP/공/방) |
|---|---|---|---|---|---|---|
| 10 | 분노한 모시 | 11 | 346 | 61 | 62 | 1.5 / 1.3 / 1.2 |
| 20 | 격노한 모치 | 20 | 450 | 124 | 94 | 1.2 / 1.05 / 1.15 |
| 30 | 고대의 프리로 | 31 | 632 | 187 | 117 | 1.45 / 1.7 / 1.2 |
| 40 | 전설의 모왕 | 40 | 787 | 233 | 175 | 1.25 / 1.1 / 1.25 |
| 50 | 오름 | 51 | 1140 | 287 | 178 | 1.5 / 1.15 / 1.05 |

배수가 층마다 들쭉날쭉한 이유가 있습니다. **기반 종족의 성향이 제각각**이라 같은 배수를 곱하면
난이도가 균일하지 않습니다 — 30층 프리로는 기본 공격이 20짜리 방어형이라 배수를 1.7까지 올려야
위협이 되고, 40층 모왕은 기본 공격 95짜리 화력형이라 1.1만 곱해도 충분합니다.
**배수를 만질 때는 종족 기본 스탯을 함께 보세요.**

### 4.4 조준한 난이도

`scripts/sim/bossCurve.ts`로 잰 "해당 층 레벨 파티 + rare 장비"의 보스 승률입니다.
목표는 **층 레벨에서 60~80%** — 한두 번 지고 넘어가는 느낌.

| 층 | 층 레벨에서의 승률 |
|---|---|
| 10 | 100% (첫 보스라 관문 역할만) |
| 20 | 70% |
| 30 | 90% |
| 40 | 80% |
| 50 | 43% → 2레벨만 더 올리면 73% |

## 4.5 밸런스 시뮬레이터 (`scripts/sim/`)

수치를 바꿀 때 감으로 정하지 않기 위한 도구입니다. 전투 계산·층 구성·아티팩트 수치는 **실제 소스를
그대로 import**하고, React 컴포넌트에 갇혀 있어 import할 수 없는 것(전투 턴 루프, 숲 노드, 드랍
테이블)만 원본과 1:1로 옮겨 적었습니다. 게임 코드를 고치면 시뮬레이션 결과도 따라 바뀝니다.

```bash
npx tsx scripts/sim/run.ts 10     # 플레미 Lv.1 → 엔딩까지 10판, 평균 통계
npx tsx scripts/sim/bossCurve.ts  # 보스층 승률이 파티 레벨에 따라 어떻게 변하는지
npx tsx scripts/sim/diagnose.ts   # 보스 실효 스펙 / 재료 획득처 점검
node  scripts/sim/sweep.mjs       # EXP_GROWTH_RATE 후보별 비교 (원본은 자동 복원)
```

`run.ts`가 내놓는 것 중 판단에 실제로 쓰이는 지표:

- **탑 전투 수** — 플레이 분량의 대리 지표. 현재 평균 223회
- **층별 패배율** — 난이도 스파이크가 어디에 몰려 있는지
- **층 도달 시점의 파티 레벨 vs 적 레벨** — 성장 곡선이 층 곡선을 따라가는지 (현재 ±2.5 이내)

주의: 시뮬레이터의 "보스 패배율"은 실제보다 부풀려 보입니다. 시뮬은 지면 직전 층 3회만 파밍하고
바로 재도전하는 단순한 정책이라 실패 시도가 많이 찍힙니다. **총 전투 수**를 보는 게 맞습니다.

---

## 5. 자동 플레이(E2E) 하네스

엔딩까지의 진행선이 실제로 끊기지 않는지 확인하기 위해 Playwright 기반 자동 플레이를 추가했습니다.

```bash
npm run e2e            # 헤드리스로 1층 → 50층 → 엔딩 완주
npm run e2e:headed     # 브라우저를 띄워 눈으로 확인
```

- 설정: `playwright.config.ts` — `npm run dev`(Vite, 5173)를 `webServer`로 자동 기동합니다. 백엔드는 필요 없습니다.
- 스크립트
  - `e2e/autoBattle.ts` — 전투 화면을 자동으로 굴리는 공용 헬퍼 (기술 선택 / 로그 넘기기 / 물약 / 교체)
  - `e2e/playthrough.spec.ts` — 1층 → 50층 → 엔딩 완주 (진행선 회귀 확인)
  - `e2e/devPresetBoss.spec.ts` — 개발자 프리셋과 같은 구성으로 50층 보스전만 (밸런스 회귀 확인).
    베이스캠프에서 탑까지 실제로 걸어가 `E` → 층 선택 모달까지 UI로 통과합니다.
- 결과물: `e2e/artifacts/` 에 층별 스크린샷과 엔딩 스크린샷이 남습니다.

동작 방식은 아래와 같습니다. 게임 코드는 전혀 건드리지 않고 **바깥에서 브라우저만 조작**합니다.

1. `addInitScript`로 `monster-rpg-auth`(게스트 세션)와 `monster-rpg-player`(테스트 파티 + 물약)를 주입합니다.
   3.3에서 설명한 대로 `{ id, level }`만 넣으면 능력치는 게임이 알아서 계산합니다.
2. `/battle`로 진입해 1층부터 시작합니다(3.2 — state가 없으면 1층).
3. 매 턴: 가장 위력이 높은(상성 우위가 있으면 그쪽 우선) 기술 버튼을 클릭하고, 로그가 뜨는 동안 `q`를 눌러 진행합니다.
4. HP가 낮으면 가방에서 맥스 물약을, 기절하면 파티 교체를 선택합니다.
5. 승리 오버레이에서 "다음층"을 눌러 체인을 이어가고, 50층에서는 "정수를 들고 마을로"를 눌러 `/ending`에 도달하면
   `THE END` 텍스트를 확인하고 종료합니다.

**테스트 파티는 의도적으로 강하게 설정되어 있습니다.** 4장의 계산대로 정상 난이도로는 50층 완주에 아티팩트 파밍과
수십 분의 플레이가 필요한데, 이 스크립트의 목적은 밸런스 검증이 아니라 **"진행선이 코드상 엔딩까지 끊기지 않는가"의 회귀 확인**이기 때문입니다.
밸런스를 검증하고 싶다면 `e2e/playthrough.spec.ts` 상단의 `PARTY_LEVEL`을 낮춰서 어디서 막히는지 보면 됩니다.

---

## 6. 다음에 할 만한 일

우선순위 순으로. (처리한 것은 8~10장에 정리돼 있습니다.)

1. **전투 로그 자동 진행** — 로그 한 줄마다 `Q`/클릭을 기다립니다. 엔딩까지 대략 8,000번입니다.
   자동 진행 토글 + 속도 조절이 지금 남은 UX 항목 중 체감이 가장 큽니다(10장 참고).
2. **제작 일괄 처리** — 물약·아티팩트 하나마다 미니게임을 합니다. 한 판에 약 96회입니다.
   "N개 한 번에 만들기"(품질은 한 번의 결과로 일괄 적용)가 자연스러운 해법입니다.
3. **초반 포획 운 편차** — 시뮬 10판에서 한 판의 길이가 94~553전투로 벌어지는데,
   가장 큰 원인이 진화 계열(아쿠비·모시)을 일찍 잡느냐입니다. 얕은 숲 포획 풀이나
   조우율을 손보면 편차가 줄어듭니다.
4. **엔딩 후 컨텐츠** — `tower_cleared` 플래그는 저장되고 탑 모달에 배지도 뜨지만
   그 이상(엔딩 재감상, 클리어 후 요소, NPC 대사 변화)은 없습니다. 플래그는 이미 있으니 붙이기만 하면 됩니다.
5. **E2E 커버리지 확대** — 지금은 탑 진행선과 50층 밸런스만 봅니다. 숲 탐험 / 공방 제작 /
   로그인·세이브 동기화는 자동 검증이 없습니다. 특히 서버 동기화는 회귀가 눈에 안 띄어 가치가 높습니다.
6. **디자인** — 11장에 점검 결과가 있습니다. 아직 손대지 않았습니다.

---

## 7. 함정 모음 (미리 알았으면 좋았을 것들)

- **`/battle` 직접 진입은 항상 1층** — 3.2 참고.
- **`ormr`는 특별 취급이 여러 군데 흩어져 있다** — 포획 불가(`canShowCatch`), 도감 완성률 제외,
  개발자 프리셋 지급 대상 제외, 처치 시 `ormr_essence` 확정 드랍. 몬스터를 추가/삭제할 때 이 예외들을 같이 살펴야 합니다.
- **"탑의 비밀" 연출은 학습 테이블과 연동된다** — `TOWER_SECRET_REVEALS`의 `moveIds`는 해당 종의 `LEARNSET`에
  **없어야** 합니다. 있으면 "이상 기술" 연출 자체가 성립하지 않습니다(`isAnomalyMove`). 학습 테이블을 손볼 때 같이 확인하세요.
- **HP는 층 사이에 이어진다** — 전투 승리 시 `updatePartyMember`로 현재 HP가 저장됩니다. 회복 수단은 물약,
  또는 `/monsters` 화면의 회복 버튼(`restorePartyHp`)뿐입니다.
- **장비 HP 보너스만 저장 경로가 다르다** — 공/방/속/치명/속성은 데미지 계산 시점에만 임시로 더하지만,
  HP는 전투 진입 시 `withOwnedHpBonus`로 미리 반영한 뒤 저장 시 다시 빼냅니다(`BattlePage.tsx`). 세이브에 보너스가
  새어들지 않게 하려는 처리라, 장비 스탯을 추가할 때 이 비대칭을 기억해야 합니다.
- **백엔드는 `.env` 없이는 안 뜬다** — `server/.env.example`을 복사해야 합니다. 게스트 모드로 프론트만 볼 때는 불필요.
- **세이브가 꼬이면** 브라우저 로컬 스토리지의 `monster-rpg-player`(+ 필요시 `monster-rpg-auth`)를 지우세요.

---

## 8. 이번 인수인계에서 처리한 것

기준 커밋(`b690234`) 이후 이 문서를 쓰면서 함께 고친 것들입니다. 왜 고쳤는지가 남아 있어야 다시 되돌리지 않으므로 적어둡니다.

**크래시 / 데이터 손상**

- `BaseCampPage`의 `DexDetail`에서 훅이 조건부 return 뒤에 호출되고 있었습니다. 존재하지 않는 몬스터 id로
  들어오면 렌더마다 훅 개수가 달라져 React가 죽는 자리라, 훅을 early return 위로 올렸습니다.
- 서버의 async 라우트가 Express 4에서 거부를 잡지 못해, DB 오류가 나면 unhandled rejection이 되고
  요청은 응답 없이 매달렸습니다. `server/src/asyncHandler.ts`로 전부 감싸고 최종 에러 미들웨어를 붙였습니다.
- `useSaveSync`가 초기 pull이 끝나기 전에도 업로드를 예약할 수 있었습니다. 새 기기에서 로그인하면
  서버 세이브를 받아오기 전의 로컬 상태로 덮어쓸 수 있는 경로라, pull이 끝날 때까지 업로드를 막았습니다.

**눈에 보이는 결함**

- 고대숲 배경의 별 60개를 렌더마다 `Math.random()`으로 다시 뽑고 있어, 상태가 바뀔 때마다 배경이 통째로 튀었습니다.
- 로그인/회원가입에 요청 제한이 없었습니다(`server/src/middleware/rateLimit.ts`, IP당 15분/20회).
  비밀번호 최소 길이가 4자라 대입 공격에 특히 취약했습니다.
- 세이브 업로드가 `express.json()` 기본 한도 100kb에 막혀 조용히 실패할 수 있었습니다. 1mb로 올리고,
  세이브 라우트에서 길이·JSON 형태를 검사해 깨진 문자열이 저장되지 않게 했습니다.

**설계 의도 미달**

- `loadDevPreset`이 "50층 테스트 대응"을 표방하면서 실제로는 50층을 이길 수 없는 구성이었습니다
  (게다가 파티 3마리가 `monsters.ts` 앞 3종 = 초반 스타터였습니다). 최종 진화체 3마리 + 만렙 엘리트
  아티팩트 풀세트 + 물약을 주도록 고치고, 실제로 이기는지 `e2e/devPresetBoss.spec.ts`로 못 박았습니다.
- 엔딩 도달이 세이브에 아무 흔적도 남기지 않았습니다. `tower_cleared` 플래그를 추가해 탑 모달에
  "정복 완료" 배지를 띄웁니다. (기존 `floor_50`은 bestFloor 파생이라 "엔딩을 봤는가"와는 다릅니다.)
- `ForestPage`의 `markCleared`가 사용처보다 뒤에 선언돼 있었습니다(TDZ 위험). 순서를 바꾸고 의존성에 넣었습니다.

**위생**

- `npm run lint` 54건 → **경고 3건**(전부 `exhaustive-deps`, 6장 5번 참고). 규칙을 끄지 않고 코드를 고쳤습니다.
  - 렌더 중 난수: `useMemo(() => ..., [])` → `useState(() => ...)` (규칙이 인정하는 "한 번만 계산" 패턴)
  - `useRef(Date.now())` → 타이머 시작 시점에 기록 / 렌더 중 ref 읽기 → 완료 시점에 상태로 확정
  - effect 내 setState → 이벤트 핸들러로 이동하거나, 불필요한 효과라 제거
  - eslint 설정에 `_` 접두사 무시 규칙(코드베이스의 기존 관례)과 e2e용 Node 전역을 추가
- 번들이 1.75MB 단일 청크였습니다. `manualChunks`로 phaser(1.2MB)/react(47kB)/앱(494kB)을 갈라,
  게임 코드만 고쳤을 때 사용자가 Phaser를 다시 받지 않게 했습니다.

**마무리 정리**

- 물약 재고가 두 곳(`potions`/`craftedPotions`)에 있는데 전투 소모(`usePotion`)가 한쪽만 줄여,
  전투에서 쓸수록 가방 표시 수량이 실제보다 많아졌습니다. 같이 줄이도록 고쳤습니다(낮은 등급부터 소모).
- 관리자 비밀키 비교를 `crypto.timingSafeEqual`로 교체했습니다.
- 남아 있던 `react-hooks/exhaustive-deps` 경고 3건을 정리해 **lint가 완전히 깨끗해졌습니다**
  (`advanceNpcDialogue`를 `useCallback`으로 고정, 전투 콜백의 누락/불필요 의존성 정리).
- `AnvilModal`의 `TODO: 향후 강화 실패 확률`을 구현했습니다. +0→+1은 확정이고 이후 90/75/60/45%로
  내려갑니다. **실패해도 강화 수치는 내려가지 않고 재료만 소모**됩니다 — 되돌릴 수 없는 손실은 만들지 않았습니다.
  시뮬레이션으로 재측정한 결과 클리어율 100%는 유지되고 평균 전투 수도 그대로입니다(223 → 215).

---

## 9. 난이도 재설계 (시뮬레이션 10판 기준)

### 9.1 손대기 전 상태

`scripts/sim/run.ts`로 "연금술·아티팩트·모루를 전부 쓰는 플레이어"가 플레미 Lv.1부터 엔딩까지
가는 판을 10회 돌린 결과는 **클리어율 0%**였습니다. 원인은 보스 수치가 아니라 성장 구조였습니다.

| 막혀 있던 것 | 결과 |
|---|---|
| `slime_extract` 획득처가 아예 없음 | 그걸 요구하는 오리온 퀘스트 완료 불가 |
| `monster_essence`·`magic_dust`가 퀘스트 1회 보상뿐 | 아티팩트 3종 중 2종 영구 제작 불가 |
| `enhancement_stone`이 분해로만 획득 | 분해할 장비가 없어 모루 4기능 전부 사용 불가 |
| `LEARNSET`(레벨업 기술표)에 호출부가 없음 | 화력이 종족 기본기에 평생 고정(플레미는 100레벨이어도 위력 45) |
| `evolvesTo`/`evolvesAtLevel`에 로직이 없음 | 진화가 존재하지 않음 |
| 경험치가 마지막에 싸운 한 마리에게만 | 파티 3마리 중 2마리가 방치 → 사실상 1마리로 50층 |

성장축이 "레벨" 하나만 남았는데 요구 경험치는 `×1.2`로 지수 증가하고 적이 주는 경험치는
층수에 선형이라, 층이 오를수록 격차가 벌어져 40층 무렵엔 한 레벨에 100전투가 넘게 필요했습니다.

### 9.2 손댄 것

**구조 (막힌 것을 뚫음)**

- 숲 채집을 구역별 표로 분리 — 얕은 숲/깊은 숲/고대 숲이 각각 다른 상위 재료를 냅니다(4.2 표).
- 전투 드랍에 `monster_essence`(21층~)와 `enhancement_stone`(11층~) 추가.
- 공방에 맥스 물약·해독제·강력 전투 물약 레시피 추가 — `items.ts`에는 있는데 만들 방법이 없던 것들입니다.
- `monster/growth.ts` 신설 — 레벨업 시 학습표대로 기술 습득(4칸이 차면 가장 약한 기술과 교체),
  조건 레벨 도달 시 진화. 진화 시 능력치는 진화체 기본값 위에 레벨 증분을 다시 쌓아 계산합니다.
- 대기 파티원도 경험치의 50%를 받습니다(`BENCH_EXP_SHARE`).
- 모루 비용 완화 — 레벨업 강화석 비용 `레벨/3` → `레벨/6`, 분해 획득량 상향,
  합성 조건에서 "재료 쪽도 만렙+최대강화" 요구를 제거(성장시킨 쪽만 조건 충족하면 됨).

**수치**

- `EXP_GROWTH_RATE` 1.2 → **1.04** (`battleUtils.ts`). sweep으로 1.20/1.12/1.08/1.06/1.05/1.04를
  비교했고, 1.04에서만 클리어율 100%가 나왔습니다.
- 적 경험치 스케일 `1 + n×0.15` → `1 + n×0.22`.
- 보스 레벨 오프셋 `floor+5` → `floor+3`, 지정 보스는 층 레벨 근처로(10/20/30/40/50 → 11/20/31/40/51).
- 보스 배수를 종족 성향에 맞춰 개별 조정(4.3 표).

### 9.3 손댄 뒤 (10판)

| 지표 | 전 | 후 |
|---|---|---|
| 클리어율 | 0% (10/10 실패) | **100% (10/10 성공)** |
| 막힌 층 | 평균 20층 | 없음 |
| 탑 전투 수 | 1240 (그마저 실패) | **평균 223** (최소 94 / 최대 553) |
| 전투 턴 | 5602 | 평균 1310 |
| 제작한 아티팩트 | 0개 | 평균 60개 |
| 장비 레벨업/강화 | 0회 | 평균 54회 / 24회 |
| 층 도달 시 레벨 - 적 레벨 | -4 ~ -9 | **-2.4 ~ +2.8** |

편차가 큽니다(94~553). 원인은 **초반 포획 운** — 진화 계열(아쿠비→아쿠사, 모시→모치→모왕)을
일찍 잡으면 절반의 전투로 끝납니다. 이걸 줄이려면 얕은 숲 포획 풀에 진화 계열을 더 넣거나,
초반 조우율을 올리는 방향이 맞습니다(아직 하지 않았습니다).

---

## 10. 플레이어 관점 UX

10판 시뮬레이션과 실제 브라우저 플레이로 12건을 뽑아, **10건을 고치고 2건을 남겼습니다.**
화면 스크린샷은 `npx playwright test uxAudit` → `e2e/artifacts/ux/`에서 다시 뽑을 수 있습니다.

### 고친 것

| 문제 | 처리 | 위치 |
|---|---|---|
| 회복하려고 탑 → `/monsters` → 베이스캠프 → 탑까지 왕복 | 전투 승리·패배 화면과 탑 층 선택 모달에 "파티 HP 전회복"을 넣어 왕복 자체를 없앰 | `BattlePage`, `BaseCampPage` |
| 탑 재진입 때마다 캐릭터를 탑까지 걷기 | `Tab` 메뉴에 "무한의 탑" 추가 — 어디서든 층 선택 모달을 연다 | `BaseCampPage`의 `MenuModal` |
| 적 HP 수치가 사실상 안 보임 | 9px 어두운 갈색 → 13px 밝은 색 + 외곽선, 바 위쪽으로 이동 | `BattleScene` |
| **전투 시작 시 HP 수치가 아예 비어 있었음** | React 마운트 effect가 씬 `create()`보다 먼저 돌아 첫 `BATTLE_STATE_UPDATE`를 아무도 못 받았다. `BATTLE_READY`에서 한 번 더 보낸다 | `BattlePage` |
| 몬스터 화면에 기술·경험치가 없음 | 상태창에 경험치 바 + "Lv.N에 무엇을 배운다 / 무엇으로 진화한다" 안내 추가 | `MonstersPage` |
| 숲 포획이 단판 승부 | 한 마리당 3회까지 재도전(`CATCH_ATTEMPTS`). 실패해도 바로 사라지지 않는다 | `ForestPage` |
| 전투 중 도망 불가 | "도망" 버튼 추가. 보스층은 제외하고, 깎인 HP는 그대로 저장해 무손실 정찰이 되지 않게 함 | `BattlePage` |
| 지나간 전투 로그를 다시 못 봄 | 상단 "기록" 토글 — 최근 50줄을 접이식 패널로 | `BattlePage` |
| 저장 시점이 보이지 않음 | 우하단 자동 저장 표시(로컬/서버 구분, 실패 시 "로컬에는 보관됨") | `shared/SaveIndicator.tsx`, `shared/saveStatusStore.ts` |
| 기술 교체를 플레이어가 못 고름 | 4칸이 찼을 때 선택 창을 띄운다. `applyLevelGrowth`가 `ForgetResolver`를 받아, 전투는 플레이어에게 묻고 대기 파티원·시뮬레이터는 자동 판단을 쓴다 | `monster/growth.ts`, `BattlePage` |
| 공방 제작대 위치를 미리 알 수 없음 | 세 제작대에 상시 표식(아이콘 + 이름). 가까이 가면 흐려지고 기존 하단 안내에 자리를 넘긴다 | `WorkshopPage` |

**자동 플레이 하네스도 같이 손봤습니다.** 기술 교체 선택 창은 플레이어 입력을 기다리며 전투를
멈추므로, `e2e/autoBattle.ts`의 `resolveForgetPrompt`가 이를 감지해 응답합니다.
전투 흐름에 "입력 대기" 지점을 새로 추가하면 여기도 함께 고쳐야 자동 플레이가 굳지 않습니다.

### 남긴 것 (의도적)

**1. 전투 로그마다 키 입력** — 한 줄마다 `Q`/클릭 ACK를 기다립니다(3.1). 한 턴에 4~10줄,
엔딩까지 대략 8,000번 이상입니다. 자동 진행·속도 조절이 없습니다.
고치려면 `sendLogAndWait`의 대기 방식 자체를 바꿔야 해서(자동 진행 토글 + 남은 로그 일괄 표시)
전투 흐름 전체에 영향이 갑니다.

**2. 제작할 때마다 미니게임 강제** — 물약/아티팩트 하나마다 가위바위보 또는 방향키 QTE.
시뮬 기준 한 판에 약 96회입니다. "N개 한 번에 만들기"(품질은 한 번의 미니게임 결과로 일괄 적용)가
자연스러운 해법입니다.

---

## 11. 디자인 점검 (보류 — 아직 손대지 않음)

밸런스·UX 작업 중 화면을 보며 눈에 띈 것들입니다. **의도적으로 미뤄둔 항목이고, 코드는 하나도
건드리지 않았습니다.** 근거 스크린샷은 `npx playwright test uxAudit` → `e2e/artifacts/ux/`.

주의: 아래는 6개 화면(베이스캠프·몬스터·가방·공방·숲·전투)을 훑어본 결과일 뿐,
전 화면을 체계적으로 감사한 것이 아닙니다. 착수한다면 전체 목록부터 다시 뽑는 편이 좋습니다.

### 공간 활용 — 세 화면이 크게 빈다

- **전투**: Phaser 캔버스(960×540) 상단 약 40%가 빈 벽인데 정보(HP·기술·파티)는 전부 하단 패널에
  몰려 있어, 한 턴마다 시선이 화면 위아래로 크게 움직입니다.
- **가방**: 재료 항목이 11개인데 4열 그리드라 아래 절반이 빕니다.
- **몬스터**: 파티/상태창/보관함 3열 구조인데 보관함이 비어 있으면 우측 약 60%가 빈 채로 남습니다.

### 일관성

- **색 체계가 화면마다 다릅니다** — 가방은 초록 계열, 공방은 앰버/갈색, 몬스터·전투는 zinc+앰버.
  공용 토큰 없이 각 화면이 자기 팔레트를 들고 있습니다(`WorkshopPage`의 `C`, `AnvilModal`의 `C`,
  `MonstersPage`의 `TYPE_ACCENT` 등이 서로 독립).
- **아이콘 톤이 섞여 있습니다** — 재료 아이콘 중 약초·열매는 픽셀아트인데 가죽은 단색 사각형입니다.

### 그 밖

- **파괴적 액션이 상시 노출**: 가방의 모든 재료 카드에 `-1`/`전체`(버리기)가 항상 떠 있습니다.
  (몬스터 "놓아주기"는 2단계 확인이 있는데 재료 버리기는 즉시 실행됩니다.)
- **반응형 미검증**: 확인한 건 1280×900 하나뿐입니다. 좁은 화면·태블릿·모바일에서 어떻게 되는지
  모릅니다. Phaser 캔버스(FIT 스케일)와 React 패널이 함께 있는 전투 화면이 가장 까다로울 것입니다.

### 착수한다면 고려할 축

1. **시각 통일** — 공용 색/타이포/아이콘 토큰을 만들어 화면별 팔레트를 걷어냅니다. 레이아웃을
   건드리지 않아 위험이 가장 작습니다.
2. **레이아웃·정보 구조** — 빈 공간과 시선 이동을 손봅니다. 화면별로 구조가 바뀌므로 e2e 셀렉터가
   깨질 수 있습니다(`e2e/autoBattle.ts`는 버튼 텍스트로 요소를 찾습니다 — 텍스트를 바꾸면 같이 고쳐야 합니다).
3. **반응형** — 먼저 목표 해상도를 정해야 합니다.
